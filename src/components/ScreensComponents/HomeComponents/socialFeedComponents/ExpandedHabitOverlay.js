import { StyleSheet, Text, View } from 'react-native'

const ExpandedHabitOverlay = ({ habitData }) => {
    return (
        <View>
            <Text>ExpandedHabitOverlay</Text>
        </View>
    )
}

export default ExpandedHabitOverlay

const styles = StyleSheet.create({

})